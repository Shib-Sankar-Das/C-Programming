// WAP to input into a character array & print the array.

#include<stdio.h>
#include<conio.h>
void main()
{
	char x[10];
	printf("Enter the string = ");
	scanf("%s",&x);
	printf("The string = %s",x);
	getch();
}